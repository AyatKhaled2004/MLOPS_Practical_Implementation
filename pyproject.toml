[project]
name = "mini-project-session-one"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AyatKhaled2004", email = "ayatkhaled32721@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "mlflow>=3.15.2",
    "numpy>=2.5.2",
    "pydantic>=2.13.4",
    "pytest>=9.1.1",
    "requests>=2.34.2",
    "scikit-learn>=1.9.0",
    "streamlit>=1.62.0",
    "structlog>=26.1.0",
    "uvicorn>=0.52.4",
]

[project.scripts]
mini-project-session-one = "mini_project_session_one:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
[tool.pytest.ini_options]
pythonpath = [
  "."
]
